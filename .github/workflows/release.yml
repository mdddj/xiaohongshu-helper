name: "publish"

on:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: "macos-latest"
            args: "--target aarch64-apple-darwin"
          - platform: "macos-latest"
            args: "--target x86_64-apple-darwin"
          - platform: "ubuntu-22.04"
            args: ""
          - platform: "windows-latest"
            args: ""

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # 确保能读取到所有历史

      # --- 【新增步骤】提取更新日志 ---
      - name: Extract changelog
        id: extract_changelog
        shell: bash
        run: |
          # 获取当前的 tag 名 (例如 v0.0.3)
          TAG_NAME=${GITHUB_REF#refs/tags/}
          # 提取当前版本下的内容：从当前版本标题开始，到下一个版本标题（##）之前结束
          # 假设你的格式是 ## v0.0.x
          CHANGELOG_CONTENT=$(sed -n "/^## $TAG_NAME/,/^## /p" CHANGELOG.md | sed '1d;$d')
          
          # 处理多行字符串输出到 GitHub Action Step Output
          echo "logs<<EOF" >> $GITHUB_OUTPUT
          echo "$CHANGELOG_CONTENT" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: install dependencies (ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: install frontend dependencies
        run: pnpm install

      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: v__VERSION__
          releaseName: "xiaohongshu-helper v__VERSION__"
          # --- 【修改这里】动态引用提取的日志 ---
          releaseBody: |
            ### 📝 更新日志 / What's New
            ${{ steps.extract_changelog.outputs.logs }}

            ---

            ### 🚀 下载指南 / Download Instructions

            | 操作系统 (OS) | 安装包类型 | 📥 直接下载 (Direct Download) |
            | :--- | :--- | :--- |
            | **Windows** | `Setup.exe` | [点击下载 (x64)](https://github.com/mdddj/xiaohongshu-helper/releases/download/v__VERSION__/xiaohongshu-helper___VERSION___x64-setup.exe) |
            | **macOS (M1/M2/M3)** | `.dmg` | [点击下载 (aarch64)](https://github.com/mdddj/xiaohongshu-helper/releases/download/v__VERSION__/xiaohongshu-helper___VERSION___aarch64.dmg) |
            | **macOS (Intel)** | `.dmg` | [点击下载 (x64)](https://github.com/mdddj/xiaohongshu-helper/releases/download/v__VERSION__/xiaohongshu-helper___VERSION___x64.dmg) |
            | **Linux (Ubuntu/Debian)** | `.deb` | [点击下载 (amd64)](https://github.com/mdddj/xiaohongshu-helper/releases/download/v__VERSION__/xiaohongshu-helper___VERSION___amd64.deb) |
            | **Linux (Universal)** | `.AppImage` | [点击下载 (amd64)](https://github.com/mdddj/xiaohongshu-helper/releases/download/v__VERSION__/xiaohongshu-helper___VERSION___amd64.AppImage) |

            > **提示**：如果点击无效，请在下方 **Assets** 列表中手动下载。

            ---

            ### 🛠️ 常见问题 (Troubleshooting) - macOS

            如果您在 macOS 上打开应用时提示 **“无法打开，因为苹果无法检查其是否包含恶意软件”** 或 **“移动到废纸篓”**，请尝试执行以下命令：

            1. 打开 **终端 (Terminal)**。
            2. 输入以下命令并回车（可能需要确认开机密码）：
               ```bash
               sudo xattr -rd com.apple.quarantine "/Applications/xiaohongshu-helper.app"
               ```
            3. 重新尝试打开应用。

            ---
            *自动发布的预览版本。如有问题请反馈。*
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}